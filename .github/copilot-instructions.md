# Copilot Instructions

Read **[AGENTS.md](../AGENTS.md)** in the repo root — it is the single source of truth for this
video production system (context, pipeline, schema, rules, commands, definition of done).
Follow it exactly. Produce data (`script.json`), not engine code; a video is done only when
`npm run validate` passes.
